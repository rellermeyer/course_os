// DO NOT EDIT
// this file is generated by generate_tests.sh

#ifdef ENABLE_TESTS

#include <stdio.h>
#include <interrupt.h>
#include <test.h>

size_t global_counter = 0;

{% for group in groups %}
extern bool test_{{group.name}}(void);
{% endfor %}

void test_main() {
    {% for group in groups %}
    if (test_{{group.name}}() == TEST_FAIL) return;
    {% endfor %}
    puts("TESTS COMPLETE. Passed {{ groups | length }} test suites\n");
    //SemihostingCall(ApplicationExit);
}

#endif
