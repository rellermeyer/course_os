// DO NOT EDIT
// this file is generated by generate_tests.sh

#ifdef ENABLE_TESTS

#include <stdio.h>
#include <interrupt.h>
#include <test.h>

size_t global_counter = 0;

{% for test in tests %}
extern bool test_{{test.name}}(void);
{% endfor %}

void test_main() {
    {% for test in tests %}
    if (test_{{test.name}}() == TEST_FAIL) return;
    {% endfor %}
    puts("TESTS COMPLETE. Passed {{ tests | length }} tests\n");
    //SemihostingCall(ApplicationExit);
}

#endif
