#ifndef MATH_H
#define MATH_H

// useful macros
#define max(a, b) ((a) > (b) ? a : b)
#define min(a, b) ((a) < (b) ? a : b)
#define abs(a)    __builtin_abs(a)
//#define abs(a) ((a) > 0 ? (a) : -(a))


#endif
